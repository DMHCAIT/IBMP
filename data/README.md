# Content Data Directory

This directory stores the persistent content data for the IBMP website.

- `content.json` - Main content file (auto-generated, gitignored)

The content is managed through the admin panel at `/admin`.
